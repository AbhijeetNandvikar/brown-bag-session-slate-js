import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { condensed } from "mdx-deck/themes";
import customTheme from "./theme";
import Counter from "./components/Counter.js";
import { vsDark } from "@code-surfer/themes";
import codeSurferCustomTheme from "./code-surfer-custom-theme.js";

export const theme = {
  ...vsDark,
  ...customTheme,
};

<h4>Hello World!!!</h4>

---

<h4>What is slate js?</h4>
<ul>
  <li>
    Completely customizable framework for building rich text
    editors.
  </li>
  <li>Built on top of React</li>
</ul>

---

<h4>What problems do slate js solves?</h4>
<ul>
  <li>Editor schema is fixed and difficult to customize</li>
  <li>
    Transforming the documents programmatically was very
    convoluted
  </li>
  <li>Building complex, nested documents was impossible</li>
</ul>

---

<h4>Slate js principals</h4>
<ul>
  <li>Schema less core</li>
  <li>Nested document model</li>
  <li>Highly customizable due to its plugin system</li>
</ul>

---

<CodeSurfer theme={{...vsDark,...codeSurferCustomTheme}}>

```js title="Installation..." 2

npm install slate slate-react

yarn add react react-dom


```

```js title="Installation..." 4

npm install slate slate-react

yarn add react react-dom


```

</CodeSurfer>

---

<CodeSurfer theme={{...vsDark,...codeSurferCustomTheme}}>

```jsx title="Initailize App.tsx" 1:7
const initialValue = [
  {
    type: "paragraph",
    children: [{ text: "A line of text in a paragraph." }],
  },
];

const App = () => {
  const [editor] = useState(() =>
    withReact(createEditor())
  );

  return (
    <Slate editor={editor} value={initialValue}>
      <Editable />
    </Slate>
  );
};
```

```jsx title="Initailize App.tsx" 9:11
const initialValue = [
  {
    type: "paragraph",
    children: [{ text: "A line of text in a paragraph." }],
  },
];

const App = () => {
  const [editor] = useState(() =>
    withReact(createEditor())
  );

  return (
    <Slate editor={editor} value={initialValue}>
      <Editable />
    </Slate>
  );
};
```

```jsx title="Initailize App.tsx" 12:17
const initialValue = [
  {
    type: "paragraph",
    children: [{ text: "A line of text in a paragraph." }],
  },
];

const App = () => {
  const [editor] = useState(() =>
    withReact(createEditor())
  );

  return (
    <Slate editor={editor} value={initialValue}>
      <Editable />
    </Slate>
  );
};
```

</CodeSurfer>

---

<CodeSurfer theme={{...vsDark,...codeSurferCustomTheme}}>

```jsx title="Declare custom types" 1
type CustomElement = { type: 'paragraph'; children: CustomText[] }

type CustomText = { text: string }

declare module 'slate' {
  interface CustomTypes {
    Editor: BaseEditor & ReactEditor
    Element: CustomElement
    Text: CustomText
  }
}
```

```jsx title="Declare custom types" 3
type CustomElement = { type: 'paragraph'; children: CustomText[] }

type CustomText = { text: string }

declare module 'slate' {
  interface CustomTypes {
    Editor: BaseEditor & ReactEditor
    Element: CustomElement
    Text: CustomText
  }
}
```

```jsx title="Declare custom types" 5:11
type CustomElement = { type: 'paragraph'; children: CustomText[] }

type CustomText = { text: string }

declare module 'slate' {
  interface CustomTypes {
    Editor: BaseEditor & ReactEditor
    Element: CustomElement
    Text: CustomText
  }
}
```

</CodeSurfer>

---
